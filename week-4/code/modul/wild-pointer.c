#include <stdio.h>

int main()
{
    // Your code goes here
    int *ptr = 10;
    /*  secara sengaja memberikan sembarang value, jika tidak ada alamat memori
    dengan alamat 10 maka pointer tersebut aka menjadi wild pointer. */
    return 0;
}